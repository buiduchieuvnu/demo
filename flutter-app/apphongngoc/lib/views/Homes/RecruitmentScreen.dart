import 'package:flutter/material.dart';

class RecruitmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tuyển dụng ")),
      body: Center(child: Text("Thông tin giới thiệu")),
    );
  }
}