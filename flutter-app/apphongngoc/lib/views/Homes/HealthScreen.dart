import 'package:flutter/material.dart';

class HealthScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tư vấn sức khoẻ ")),
      body: Center(child: Text("Thông tin giới thiệu")),
    );
  }
}