import 'package:flutter/material.dart';

class CalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lịch hẹn ")),
      body: Center(child: Text("Thông tin giới thiệu")),
    );
  }
}