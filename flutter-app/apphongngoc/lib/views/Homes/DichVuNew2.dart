import 'package:flutter/material.dart';

class DichVuNew2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Đặt lịch - Xác nhận lịch tự động")),
      body: Center(child: Text("Thông tin giới thiệu")),
    );
  }
}