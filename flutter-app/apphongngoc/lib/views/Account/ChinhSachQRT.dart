import 'package:flutter/material.dart';

class chinhsachqrt extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Chính sách quyền riêng tư")),
      body: Center(child: Text("")),
    );
  }
}