import 'package:flutter/material.dart';

class chinhsachqkn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Chính sách giải quyết khiếu nại")),
      body: Center(child: Text("")),
    );
  }
}